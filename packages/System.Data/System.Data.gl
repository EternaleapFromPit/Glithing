package System.Data;

enum SqlDbType {
    VarChar = 0,
    Int = 1,
    // Add other needed members as required.
}

enum IsolationLevel {
    ReadCommitted = 0,
    Serializable = 1
}

interface IDbContextTransaction {
    void Commit();
    void Rollback();
    void Dispose();
}
